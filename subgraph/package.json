{
  "name": "staking-dapp-subgraph",
  "version": "0.1.0",
  "description": "Subgraph for Staking DApp",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio subgraph-implementation",
    "create-local": "graph create --node http://localhost:8020/ staking-dapp-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ staking-dapp-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 staking-dapp-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}